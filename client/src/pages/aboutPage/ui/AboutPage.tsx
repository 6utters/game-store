import { FC, memo } from 'react'
import { Layout } from '@/widgets/layout'

const AboutPage: FC = memo(() => (
	<Layout title={'D&D Games | AboutPage page'} withNavbar withFooter>
		About Page
	</Layout>
))

export default AboutPage
